PostMaster
==========





Author:
-------



Attributes:
-----------



Value Attributes:
-----------------

.. describe:: bufferSize (type: unsigned int, class: PostMaster)

   Size of the send a receive buffers for each node.

.. describe:: children (type: vector<Id>, class: PostMaster)

   vector of ObjIds listing all children of current object

.. describe:: className (type: string, class: PostMaster)

   Class Name of object

.. describe:: destFields (type: vector<string>, class: PostMaster)

   List of all destination fields on Element, that is, fieldsthat are accessed
   as Element functions.

.. describe:: dt (type: double, class: PostMaster)

   Timestep used for this Element. Zero if not scheduled.

.. describe:: fieldIndex (type: unsigned int, class: PostMaster)

   For a FieldElement: field Index of self.For a regular Element: zero.

.. describe:: idValue (type: unsigned int, class: PostMaster)

   Object id of self, converted to an unsigned int.

.. describe:: index (type: unsigned int, class: PostMaster)

   For a FieldElement: Object index of parent.For a regular Element: Object
   index (dataId) of self.

.. describe:: me (type: ObjId, class: PostMaster)

   ObjId for current object

.. describe:: msgIn (type: vector<ObjId>, class: PostMaster)

   Messages coming in to this Element

.. describe:: msgOut (type: vector<ObjId>, class: PostMaster)

   Messages going out from this Element

.. describe:: myNode (type: unsigned int, class: PostMaster)

   Returns index of current node.

.. describe:: name (type: string, class: PostMaster)

   Name of object

.. describe:: numData (type: unsigned int, class: PostMaster)

   # of Data entries on Element.Note that on a FieldElement this does NOT
   refer to field entries,but to the number of DataEntries on the parent
   of the FieldElement.

.. describe:: numField (type: unsigned int, class: PostMaster)

   For a FieldElement: number of entries of self.For a regular Element:
   One.

.. describe:: numNodes (type: unsigned int, class: PostMaster)

   Returns number of nodes that simulation runs on.

.. describe:: parent (type: ObjId, class: PostMaster)

   Parent ObjId for current object

.. describe:: path (type: string, class: PostMaster)

   text path for object

.. describe:: sourceFields (type: vector<string>, class: PostMaster)

   List of all source fields on Element, that is fields that can act as
   message sources.

.. describe:: this (type: 7Neutral, class: PostMaster)

   Access function for entire object

.. describe:: tick (type: int, class: PostMaster)

   Clock tick for this Element for periodic execution in the main simulation
   event loop. A default is normally assigned, based on object class, but
   one can override to any value between 0 and 19. Assigning to -1 means
   that the object is disabled and will not be called during simulation
   execution The actual timestep (dt) belonging to a clock tick is defined
   by the Clock object.

.. describe:: valueFields (type: vector<string>, class: PostMaster)

   List of all value fields on Element.These fields are accessed through
   the assignment operations in the Python interface.These fields may also
   be accessed as functions through the set<FieldName> and get<FieldName>
   commands.


Attributes inherited from Neutral:
----------------------------------


Lookup Attributes:
------------------

.. describe:: isA (type: string,bool, class: PostMaster)

   Returns true if the current object is derived from the specified the
   specified class

.. describe:: msgDestFunctions (type: string,vector<string>, class: PostMaster)

   Matching function names for each ObjId receiving a msg from the specified
   SrcFinfo

.. describe:: msgDests (type: string,vector<ObjId>, class: PostMaster)

   ObjIds receiving messages from the specified SrcFinfo

.. describe:: neighbors (type: string,vector<Id>, class: PostMaster)

   Ids of Elements connected this Element on specified field.


Attributes inherited from Neutral:
----------------------------------


Src Attributes:
---------------

.. describe:: childOut (type: int, class: PostMaster)

   Message to child Elements


Attributes inherited from Neutral:
----------------------------------


Dest Attributes:
----------------

.. describe:: getBufferSize (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getChildren (type: vector<vector<Id>>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getClassName (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getDestFields (type: vector<vector<string>>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getDt (type: vector<double>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getFieldIndex (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIdValue (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIndex (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIsA (type: bool, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMe (type: vector<ObjId>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgDestFunctions (type: vector<string>, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgDests (type: vector<ObjId>, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgIn (type: vector<vector<ObjId>>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgOut (type: vector<vector<ObjId>>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMyNode (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getName (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNeighbors (type: vector<Id>, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumData (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumField (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumNodes (type: vector<unsigned int>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getParent (type: vector<ObjId>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getPath (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getSourceFields (type: vector<vector<string>>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getThis (type: PSt6vectorI7NeutralSaIS0_EE, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getTick (type: PSt6vectorIiSaIiEE, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getValueFields (type: vector<vector<string>>*, class: PostMaster)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: notifyAddMsgDest (type: ObjId, class: PostMaster)

   Called when a message is created, current object is dest. Arg is msgId.

.. describe:: notifyAddMsgSrc (type: ObjId, class: PostMaster)

   Called when a message is created, current object is src. Arg is msgId.

.. describe:: notifyCopy (type: ObjId, class: PostMaster)

   Called when object is copied. Arg is original.

.. describe:: notifyCreate (type: ObjId, class: PostMaster)

   Called when object is created. Arg is parent.

.. describe:: notifyDestroy (type: void, class: PostMaster)

   Called when object is destroyed.

.. describe:: notifyMove (type: ObjId, class: PostMaster)

   Called when object is moved. Arg is new parent.

.. describe:: parentMsg (type: int, class: PostMaster)

   Message from Parent Element(s)

.. describe:: process (type: PK8ProcInfo, class: PostMaster)

   Handles process call

.. describe:: reinit (type: PK8ProcInfo, class: PostMaster)

   Handles reinit call

.. describe:: setBufferSize (type: unsigned int, class: PostMaster)

   Assigns field value.

.. describe:: setName (type: string, class: PostMaster)

   Assigns field value.

.. describe:: setNumData (type: unsigned int, class: PostMaster)

   Assigns field value.

.. describe:: setNumField (type: unsigned int, class: PostMaster)

   Assigns field value.

.. describe:: setThis (type: 7Neutral, class: PostMaster)

   Assigns field value.

.. describe:: setTick (type: int, class: PostMaster)

   Assigns field value.


Attributes inherited from Neutral:
----------------------------------


Shared Attributes:
------------------

.. describe:: proc (type: void, class: PostMaster)

   Shared message for process and reinit


Attributes inherited from Neutral:
----------------------------------

