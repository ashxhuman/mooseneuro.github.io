SpikeStats
==========


Object to do some minimal stats on rate of a spike train. Derived from the Stats object and returns the same set of stats.Can take either predigested spike event input, or can handle a continuous sampling of membrane potential Vm and decide if a spike has occured based on a threshold.

Author:   Upi Bhalla Aug 2014


Attributes:
-----------



Value Attributes:
-----------------

.. describe:: children (type: vector<Id>, class: SpikeStats)

   vector of ObjIds listing all children of current object

.. describe:: className (type: string, class: SpikeStats)

   Class Name of object

.. describe:: destFields (type: vector<string>, class: SpikeStats)

   List of all destination fields on Element, that is, fieldsthat are accessed
   as Element functions.

.. describe:: dt (type: double, class: SpikeStats)

   Timestep used for this Element. Zero if not scheduled.

.. describe:: fieldIndex (type: unsigned int, class: SpikeStats)

   For a FieldElement: field Index of self.For a regular Element: zero.

.. describe:: idValue (type: unsigned int, class: SpikeStats)

   Object id of self, converted to an unsigned int.

.. describe:: index (type: unsigned int, class: SpikeStats)

   For a FieldElement: Object index of parent.For a regular Element: Object
   index (dataId) of self.

.. describe:: me (type: ObjId, class: SpikeStats)

   ObjId for current object

.. describe:: mean (type: double, class: SpikeStats)

   Mean of all sampled values or of spike rate.

.. describe:: msgIn (type: vector<ObjId>, class: SpikeStats)

   Messages coming in to this Element

.. describe:: msgOut (type: vector<ObjId>, class: SpikeStats)

   Messages going out from this Element

.. describe:: name (type: string, class: SpikeStats)

   Name of object

.. describe:: num (type: unsigned int, class: SpikeStats)

   Number of all sampled values, or total number of spikes.

.. describe:: numData (type: unsigned int, class: SpikeStats)

   # of Data entries on Element.Note that on a FieldElement this does NOT
   refer to field entries,but to the number of DataEntries on the parent
   of the FieldElement.

.. describe:: numField (type: unsigned int, class: SpikeStats)

   For a FieldElement: number of entries of self.For a regular Element:
   One.

.. describe:: parent (type: ObjId, class: SpikeStats)

   Parent ObjId for current object

.. describe:: path (type: string, class: SpikeStats)

   text path for object

.. describe:: sdev (type: double, class: SpikeStats)

   Standard Deviation of all sampled values, or of rate.

.. describe:: sourceFields (type: vector<string>, class: SpikeStats)

   List of all source fields on Element, that is fields that can act as
   message sources.

.. describe:: sum (type: double, class: SpikeStats)

   Sum of all sampled values, or total number of spikes.

.. describe:: this (type: 7Neutral, class: SpikeStats)

   Access function for entire object

.. describe:: threshold (type: double, class: SpikeStats)

   Spiking threshold. If Vm crosses this going up then the SpikeStats object
   considers that a spike has happened and adds it to the stats.

.. describe:: tick (type: int, class: SpikeStats)

   Clock tick for this Element for periodic execution in the main simulation
   event loop. A default is normally assigned, based on object class, but
   one can override to any value between 0 and 19. Assigning to -1 means
   that the object is disabled and will not be called during simulation
   execution The actual timestep (dt) belonging to a clock tick is defined
   by the Clock object.

.. describe:: valueFields (type: vector<string>, class: SpikeStats)

   List of all value fields on Element.These fields are accessed through
   the assignment operations in the Python interface.These fields may also
   be accessed as functions through the set<FieldName> and get<FieldName>
   commands.

.. describe:: windowLength (type: unsigned int, class: SpikeStats)

   Number of bins for windowed stats. Ignores windowing if this value is
   zero.

.. describe:: wmean (type: double, class: SpikeStats)

   Mean of sampled values or of spike rate within window.

.. describe:: wnum (type: unsigned int, class: SpikeStats)

   Number of all sampled values, or total number of spikes, within window.

.. describe:: wsdev (type: double, class: SpikeStats)

   Standard Deviation of sampled values, or rate, within window.

.. describe:: wsum (type: double, class: SpikeStats)

   Sum of all sampled values, or total number of spikes, within window.


Attributes inherited from Stats:
--------------------------------


Attributes inherited from Neutral:
----------------------------------


Lookup Attributes:
------------------

.. describe:: isA (type: string,bool, class: SpikeStats)

   Returns true if the current object is derived from the specified the
   specified class

.. describe:: msgDestFunctions (type: string,vector<string>, class: SpikeStats)

   Matching function names for each ObjId receiving a msg from the specified
   SrcFinfo

.. describe:: msgDests (type: string,vector<ObjId>, class: SpikeStats)

   ObjIds receiving messages from the specified SrcFinfo

.. describe:: neighbors (type: string,vector<Id>, class: SpikeStats)

   Ids of Elements connected this Element on specified field.


Attributes inherited from Stats:
--------------------------------


Attributes inherited from Neutral:
----------------------------------


Src Attributes:
---------------

.. describe:: childOut (type: int, class: SpikeStats)

   Message to child Elements

.. describe:: requestOut (type: vector<double>*, class: SpikeStats)

   Sends request for a field to target object


Attributes inherited from Stats:
--------------------------------


Attributes inherited from Neutral:
----------------------------------


Dest Attributes:
----------------

.. describe:: Vm (type: double, class: SpikeStats)

   Handles continuous voltage input, can be coming in much than update rate
   of the SpikeStats. Looks for transitions above threshold to register
   the arrival of a spike. Doesn't do another spike till Vm falls below
   threshold.

.. describe:: addSpike (type: double, class: SpikeStats)

   Handles spike event time input, converts into a rate to do stats upon.

.. describe:: getChildren (type: vector<vector<Id>>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getClassName (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getDestFields (type: vector<vector<string>>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getDt (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getFieldIndex (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIdValue (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIndex (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIsA (type: bool, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMe (type: vector<ObjId>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMean (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgDestFunctions (type: vector<string>, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgDests (type: vector<ObjId>, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgIn (type: vector<vector<ObjId>>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgOut (type: vector<vector<ObjId>>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getName (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNeighbors (type: vector<Id>, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNum (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumData (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumField (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getParent (type: vector<ObjId>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getPath (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getSdev (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getSourceFields (type: vector<vector<string>>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getSum (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getThis (type: PSt6vectorI7NeutralSaIS0_EE, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getThreshold (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getTick (type: PSt6vectorIiSaIiEE, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getValueFields (type: vector<vector<string>>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getWindowLength (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getWmean (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getWnum (type: vector<unsigned int>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getWsdev (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getWsum (type: vector<double>*, class: SpikeStats)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: input (type: double, class: SpikeStats)

   Handles continuous value input as a time-series. Multiple inputs are
   allowed, they will be merged.

.. describe:: notifyAddMsgDest (type: ObjId, class: SpikeStats)

   Called when a message is created, current object is dest. Arg is msgId.

.. describe:: notifyAddMsgSrc (type: ObjId, class: SpikeStats)

   Called when a message is created, current object is src. Arg is msgId.

.. describe:: notifyCopy (type: ObjId, class: SpikeStats)

   Called when object is copied. Arg is original.

.. describe:: notifyCreate (type: ObjId, class: SpikeStats)

   Called when object is created. Arg is parent.

.. describe:: notifyDestroy (type: void, class: SpikeStats)

   Called when object is destroyed.

.. describe:: notifyMove (type: ObjId, class: SpikeStats)

   Called when object is moved. Arg is new parent.

.. describe:: parentMsg (type: int, class: SpikeStats)

   Message from Parent Element(s)

.. describe:: process (type: const ProcInfo*, class: SpikeStats)

   Handles process call

.. describe:: reinit (type: const ProcInfo*, class: SpikeStats)

   Handles reinit call

.. describe:: setName (type: string, class: SpikeStats)

   Assigns field value.

.. describe:: setNumData (type: unsigned int, class: SpikeStats)

   Assigns field value.

.. describe:: setNumField (type: unsigned int, class: SpikeStats)

   Assigns field value.

.. describe:: setThis (type: 7Neutral, class: SpikeStats)

   Assigns field value.

.. describe:: setThreshold (type: double, class: SpikeStats)

   Assigns field value.

.. describe:: setTick (type: int, class: SpikeStats)

   Assigns field value.

.. describe:: setWindowLength (type: unsigned int, class: SpikeStats)

   Assigns field value.


Attributes inherited from Stats:
--------------------------------


Attributes inherited from Neutral:
----------------------------------


Shared Attributes:
------------------

.. describe:: proc (type: void, class: SpikeStats)

   Shared message for process and reinit


Attributes inherited from Stats:
--------------------------------


Attributes inherited from Neutral:
----------------------------------

