SpikeGen
========


SpikeGen object, for detecting threshold crossings.The threshold detection can work in multiple modes.
If the refractT < 0.0, then it fires an event only at the rising edge of the input voltage waveform

Author:   Upi Bhalla


Attributes:
-----------

===========


Value Attributes:
-----------------

-----------------
.. describe:: abs_refract (type: double, class: SpikeGen)

   Absolute refractory time. Synonym for refractT.

.. describe:: children (type: vector<Id>, class: SpikeGen)

   vector of ObjIds listing all children of current object

.. describe:: className (type: string, class: SpikeGen)

   Class Name of object

.. describe:: destFields (type: vector<string>, class: SpikeGen)

   List of all destination fields on Element, that is, fieldsthat are accessed
   as Element functions.

.. describe:: dt (type: double, class: SpikeGen)

   Timestep used for this Element. Zero if not scheduled.

.. describe:: edgeTriggered (type: bool, class: SpikeGen)

   When edgeTriggered = 0, the SpikeGen will fire an event in each timestep
   while incoming Vm is > threshold and at least abs_refracttime has passed
   since last event. This may be problematic if the incoming Vm remains
   above threshold for longer than abs_refract. Setting edgeTriggered to
   1 resolves this as the SpikeGen generatesan event only on the rising
   edge of the incoming Vm and will remain idle unless the incoming Vm goes
   below threshold.

.. describe:: fieldIndex (type: unsigned int, class: SpikeGen)

   For a FieldElement: field Index of self.For a regular Element: zero.

.. describe:: hasFired (type: bool, class: SpikeGen)

   True if SpikeGen has just fired

.. describe:: idValue (type: unsigned int, class: SpikeGen)

   Object id of self, converted to an unsigned int.

.. describe:: index (type: unsigned int, class: SpikeGen)

   For a FieldElement: Object index of parent.For a regular Element: Object
   index (dataId) of self.

.. describe:: lastSpikeTime (type: double, class: SpikeGen)

   Time of most recent spike event.

.. describe:: me (type: ObjId, class: SpikeGen)

   ObjId for current object

.. describe:: msgIn (type: vector<ObjId>, class: SpikeGen)

   Messages coming in to this Element

.. describe:: msgOut (type: vector<ObjId>, class: SpikeGen)

   Messages going out from this Element

.. describe:: name (type: string, class: SpikeGen)

   Name of object

.. describe:: numData (type: unsigned int, class: SpikeGen)

   # of Data entries on Element.Note that on a FieldElement this does NOT
   refer to field entries,but to the number of DataEntries on the parent
   of the FieldElement.

.. describe:: numField (type: unsigned int, class: SpikeGen)

   For a FieldElement: number of entries of self.For a regular Element:
   One.

.. describe:: parent (type: ObjId, class: SpikeGen)

   Parent ObjId for current object

.. describe:: path (type: string, class: SpikeGen)

   text path for object

.. describe:: refractT (type: double, class: SpikeGen)

   Refractory Time.

.. describe:: sourceFields (type: vector<string>, class: SpikeGen)

   List of all source fields on Element, that is fields that can act as
   message sources.

.. describe:: this (type: 7Neutral, class: SpikeGen)

   Access function for entire object

.. describe:: threshold (type: double, class: SpikeGen)

   Spiking threshold, must cross it going up

.. describe:: tick (type: int, class: SpikeGen)

   Clock tick for this Element for periodic execution in the main simulation
   event loop. A default is normally assigned, based on object class, but
   one can override to any value between 0 and 19. Assigning to -1 means
   that the object is disabled and will not be called during simulation
   execution The actual timestep (dt) belonging to a clock tick is defined
   by the Clock object.

.. describe:: valueFields (type: vector<string>, class: SpikeGen)

   List of all value fields on Element.These fields are accessed through
   the assignment operations in the Python interface.These fields may also
   be accessed as functions through the set<FieldName> and get<FieldName>
   commands.


Attributes inherited from Neutral:
----------------------------------


Lookup Attributes:
------------------

------------------
.. describe:: isA (type: string,bool, class: SpikeGen)

   Returns true if the current object is derived from the specified the
   specified class

.. describe:: msgDestFunctions (type: string,vector<string>, class: SpikeGen)

   Matching function names for each ObjId receiving a msg from the specified
   SrcFinfo

.. describe:: msgDests (type: string,vector<ObjId>, class: SpikeGen)

   ObjIds receiving messages from the specified SrcFinfo

.. describe:: neighbors (type: string,vector<Id>, class: SpikeGen)

   Ids of Elements connected this Element on specified field.


Attributes inherited from Neutral:
----------------------------------


Src Attributes:
---------------

---------------
.. describe:: childOut (type: int, class: SpikeGen)

   Message to child Elements

.. describe:: spikeOut (type: double, class: SpikeGen)

   Sends out a trigger for an event.


Attributes inherited from Neutral:
----------------------------------


Dest Attributes:
----------------

----------------
.. describe:: Vm (type: double, class: SpikeGen)

   Handles Vm message coming in from compartment

.. describe:: getAbs_refract (type: vector<double>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getChildren (type: vector<vector<Id>>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getClassName (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getDestFields (type: vector<vector<string>>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getDt (type: vector<double>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getEdgeTriggered (type: vector<bool>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getFieldIndex (type: vector<unsigned int>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getHasFired (type: vector<bool>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIdValue (type: vector<unsigned int>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIndex (type: vector<unsigned int>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getIsA (type: bool, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getLastSpikeTime (type: vector<double>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMe (type: vector<ObjId>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgDestFunctions (type: vector<string>, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgDests (type: vector<ObjId>, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgIn (type: vector<vector<ObjId>>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getMsgOut (type: vector<vector<ObjId>>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getName (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNeighbors (type: vector<Id>, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumData (type: vector<unsigned int>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getNumField (type: vector<unsigned int>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getParent (type: vector<ObjId>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getPath (type: PSt6vectorINSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEESaIS5_EE, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getRefractT (type: vector<double>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getSourceFields (type: vector<vector<string>>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getThis (type: PSt6vectorI7NeutralSaIS0_EE, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getThreshold (type: vector<double>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getTick (type: PSt6vectorIiSaIiEE, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: getValueFields (type: vector<vector<string>>*, class: SpikeGen)

   Requests field value. The requesting Element must provide a handler for
   the returned value.

.. describe:: notifyAddMsgDest (type: ObjId, class: SpikeGen)

   Called when a message is created, current object is dest. Arg is msgId.

.. describe:: notifyAddMsgSrc (type: ObjId, class: SpikeGen)

   Called when a message is created, current object is src. Arg is msgId.

.. describe:: notifyCopy (type: ObjId, class: SpikeGen)

   Called when object is copied. Arg is original.

.. describe:: notifyCreate (type: ObjId, class: SpikeGen)

   Called when object is created. Arg is parent.

.. describe:: notifyDestroy (type: void, class: SpikeGen)

   Called when object is destroyed.

.. describe:: notifyMove (type: ObjId, class: SpikeGen)

   Called when object is moved. Arg is new parent.

.. describe:: parentMsg (type: int, class: SpikeGen)

   Message from Parent Element(s)

.. describe:: process (type: const ProcInfo*, class: SpikeGen)

   Handles process call

.. describe:: reinit (type: const ProcInfo*, class: SpikeGen)

   Handles reinit call

.. describe:: setAbs_refract (type: double, class: SpikeGen)

   Assigns field value.

.. describe:: setEdgeTriggered (type: bool, class: SpikeGen)

   Assigns field value.

.. describe:: setName (type: string, class: SpikeGen)

   Assigns field value.

.. describe:: setNumData (type: unsigned int, class: SpikeGen)

   Assigns field value.

.. describe:: setNumField (type: unsigned int, class: SpikeGen)

   Assigns field value.

.. describe:: setRefractT (type: double, class: SpikeGen)

   Assigns field value.

.. describe:: setThis (type: 7Neutral, class: SpikeGen)

   Assigns field value.

.. describe:: setThreshold (type: double, class: SpikeGen)

   Assigns field value.

.. describe:: setTick (type: int, class: SpikeGen)

   Assigns field value.


Attributes inherited from Neutral:
----------------------------------


Shared Attributes:
------------------

------------------
.. describe:: proc (type: void, class: SpikeGen)

   Shared message to receive Process message from scheduler


Attributes inherited from Neutral:
----------------------------------

